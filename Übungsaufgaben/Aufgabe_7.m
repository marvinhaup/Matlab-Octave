% Aufgabe 7 der Übungsaufgaben

werte = [2,5,7,8,1,-2,4,6];
daten = [4,6,2,1]; r = 4; s = 1; t = 7;
x = Aufruf(werte,r,t);
y = Aufruf(werte,s,r);
z = werte(8);
fprintf('x: %i\n', x);
fprintf('y: %i\n', y);
fprintf('z: %i\n', z);
